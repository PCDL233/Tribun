# 基于 Vibe Coding 的 Git 提交 AI Agent 代码审查与风险分析系统 — 技术实现方案与项目报告书

> **技术栈基线（2026-09 优化版）**：全栈 TypeScript（6.x）· React 19 · Vite 8（Rolldown 内核）· pnpm 10 Monorepo · Turborepo · Node.js 22 LTS · Hono 4 · LangGraph.js
> 本次优化将原 Python/Node 混合栈统一为**前后端全 TypeScript 单语言栈**，并重新确认了核心业务链路（详见 3.0 节）。

## 第一部分：技术实现方案

### 一、项目背景与设计理念

#### 1.1 Vibe Coding 在 2026 年的工程化演进

Vibe Coding 在 2026 年已从"凭感觉让 AI 写代码"演变为结构化的 AI 优先开发方法论。其核心工作流是 Prompt → Generate → Review → Refine 四阶段循环，其中 Review 和 Refine 阶段消耗了最多的人力投入，也是价值最高的环节。在这一范式下，开发者的角色从逐行编码者转变为架构师和审查者，代码审查的方式也从"逐行阅读"变为"审查 diff 变更 + 核心逻辑校验"。

本项目的核心理念正是将 Vibe Coding 工作流中的 Review 阶段自动化、智能化：当开发者执行 `git commit` 时，AI Agent 自动对暂存区的代码变更进行审查，识别缺陷、评估风险，并输出结构化报告，在代码进入仓库之前就完成第一轮质量把关。

#### 1.2 当前代码审查的核心痛点

当前代码审查的核心痛点
传统代码审查面临以下关键挑战：

**人工审查的瓶颈**：人工审查在速度和一致性上存在明显瓶颈，审查质量高度依赖审查者的经验和精力。一个包含 500 行变更的 PR 可能需要数十分钟的人工审查。

**静态分析工具的局限**：传统 SAST 工具（如 CodeQL、Snyk）在漏洞检测上的平均 F1 分数仅为 0.260~~0.546，而基于 LLM 的扫描工具 F1 分数可达 0.750~~0.797。静态分析工具受限于规则匹配，难以理解代码语义和业务逻辑。

**LLM 审查的幻觉问题**：GenAI 代码审查工具的平均幻觉率为 18%，没有工具在注入类漏洞上达到 70% 的召回率。关键在于将 LLM 的判断与确定性分析工具结合，降低误报。

**上下文不足导致的漏报**：业务逻辑类漏洞（如权限校验缺失）的平均召回率仅为 21%，但当为 LLM 提供约 2000 token 的项目背景描述后，召回率可提升至 51%。这证实了 RAG 知识库在代码审查中的必要性。

#### 1.3 设计原则

本项目的核心理念正是将 Vibe Coding 工作流中的 Review 阶段自动化、智能化：当开发者执行 `git commit` 时，AI Agent 自动对暂存区的代码变更进行审查，识别缺陷、评估风险，并输出结构化报告，在代码进入仓库之前就完成第一轮质量把关。

- **混合分析原则**：LLM 语义理解与静态分析工具互补，通过交叉验证降低误报和幻觉。
- **增量审查原则**：仅审查 diff 变更内容，而非全量代码，以 AgenticSCR 的 diff-centric 范式为基础。
- **确定性优先原则**：确定性工具能判定的问题（如语法错误、复杂度超标）不交给 LLM，减少 token 浪费和不确定性。
- **可审计原则**：每次审查的输入、推理过程、工具调用和输出均完整记录，支持事后追溯。

#### 1.4 全栈 TypeScript 单语言栈的选型理由

本项目将 CLI、Agent 编排、API Server、Web Dashboard、MCP 工具全部统一到 TypeScript，理由如下：

1. **单语言降低认知与维护成本**：从 Git Hook 触发的 CLI，到编排引擎，再到 Dashboard 前端，全链路一种语言、一套工具链（pnpm + Turborepo 统一管理），避免了原方案 Python/Node 双栈带来的进程调用、序列化边界和双重依赖维护。
2. **AI/Agent 生态的 TypeScript 一等公民地位**：Model Context Protocol 官方 SDK（`@modelcontextprotocol/sdk`）、Vercel AI SDK 5（统一 LLM Provider、内置工具调用循环与流式输出）均以 TypeScript 优先发布，本项目的两大核心依赖无需任何跨语言桥接。
3. **端到端类型安全**：以 zod schema 为单一事实源，配置校验 → API 契约 → 数据库行类型 → 前端表单全链路推导自同一份定义，契约漂移在编译期即被发现。
4. **分发与跨平台优势**：产物为纯 Node.js 包（可单文件 bundle），无 Python 解释器/虚拟环境碎片化问题；对 Windows、macOS、Linux 一致支持——这对 Git Hook 场景尤为重要（团队成员操作系统各异）。
5. **代码分析主战场即 JS/TS**：`ts-morph`、`oxc-parser` 对 JS/TS 提供第一方 AST 能力；其他语言（Python/Go/Java）通过 Tree-sitter 语法包覆盖。

### 二、系统总体架构

系统采用四层架构设计，自上而下分为交互层、Agent 编排层、分析引擎层和数据层，全部以 TypeScript 实现：

```
┌────────────────────────────────────────────────────────────────────────────┐
│                     交互层 (Interaction Layer) — 全 TypeScript              │
│  ┌───────────────┐  ┌────────────────┐  ┌──────────────┐  ┌────────────┐  │
│  │ Git Hook CLI  │  │ Web Dashboard  │  │ API Server   │  │ IDE Plugin │  │
│  │ commander     │  │ React 19       │  │ Hono 4       │  │ (Phase 4)  │  │
│  │ + @clack      │  │ + Vite 8       │  │ SSE + OpenAPI│  │ VS Code    │  │
│  └───────┬───────┘  └───────┬────────┘  └──────┬───────┘  └─────┬──────┘  │
└──────────┼──────────────────┼───────────────────┼────────────────┼─────────┘
           │                  │ HTTP / SSE        │                │
┌──────────▼──────────────────▼───────────────────▼────────────────▼─────────┐
│                     Agent 编排层 (Orchestration Layer)                      │
│           LangGraph.js 编排引擎（@langchain/langgraph · StateGraph）         │
│   ┌─────────┐  ┌─────────┐  ┌─────────┐  ┌──────────┐  ┌──────────┐      │
│   │ Diff    │→ │ Risk    │→ │ Parallel│→ │ Cross    │→ │ Report   │      │
│   │ Parser  │  │ Planner │  │ Review  │  │ Validate │  │ Assemble │      │
│   │ Node    │  │ Node    │  │ Nodes×3 │  │ (+自愈)  │  │ Node     │      │
│   └─────────┘  └─────────┘  └─────────┘  └──────────┘  └──────────┘      │
└──────────┬─────────────────────────────────────────────────────────────────┘
           │ 进程内函数调用（默认）/ MCP Protocol stdio·SSE（对外集成）
┌──────────▼─────────────────────────────────────────────────────────────────┐
│                      分析引擎层 (Analysis Engine Layer)                      │
│  ┌──────────────┐ ┌──────────────┐ ┌──────────────┐ ┌──────────────┐      │
│  │ LLM Agent    │ │ Static       │ │ RAG Knowledge│ │ Test Runner  │      │
│  │ AI SDK 5     │ │ Tree-sitter  │ │ LanceDB      │ │ / Sandbox    │      │
│  │ ReAct Loop   │ │ ts-morph/Sem │ │ + MiniSearch │ │ (Phase 4)    │      │
│  └──────┬───────┘ └──────┬───────┘ └──────┬───────┘ └──────┬───────┘      │
│  ┌──────▼────────────────▼────────────────▼────────────────▼───────────┐  │
│  │            工具调用总线 (In-Process Tool Bus / MCP Tool Bus)          │  │
│  │   Git Tool │ Lint Tool │ Security Scanner │ Complexity │ Dep Check   │  │
│  └─────────────────────────────────────────────────────────────────────┘  │
└──────────┬─────────────────────────────────────────────────────────────────┘
           │ Drizzle ORM / 原生 SDK
┌──────────▼─────────────────────────────────────────────────────────────────┐
│                           数据层 (Data Layer)                               │
│  ┌──────────┐ ┌──────────────┐ ┌──────────┐ ┌──────────┐ ┌────────────┐  │
│  │SQLite/   │ │ LanceDB      │ │ Redis    │ │ File     │ │ Prometheus │  │
│  │PostgreSQL│ │ (嵌入式向量库)│ │ (缓存/   │ │ Storage  │ │ prom-client│  │
│  │(Drizzle) │ │ 备选sqlite-vec│ │  任务队列)│ │ (报告)    │ │ + Grafana  │  │
│  └──────────┘ └──────────────┘ └──────────┘ └──────────┘ └────────────┘  │
└────────────────────────────────────────────────────────────────────────────┘
```

#### 2.1 各层职责

**交互层**：提供多种触发入口。Git Hook CLI 是核心入口，在 `pre-commit` 阶段自动触发审查（commander + @clack/prompts 提供终端进度反馈）；Web Dashboard 基于 React 19 + Vite 8 构建，提供审查历史、diff 视图、统计分析和配置管理；API Server 基于 Hono 4，支持 CI/CD 集成，并通过 SSE 向 Dashboard 推送实时审查进度；IDE Plugin（可选，Phase 4）提供开发时的实时反馈。

**Agent 编排层**：基于 LangGraph.js（`@langchain/langgraph`，官方 1.0 稳定版）的 StateGraph 编排引擎，定义审查流水线的各个节点及其依赖关系。共享状态以 Channel/Reducer 模型声明（`Annotation.Root`），每个节点只返回增量更新，由 reducer 自动合并——并行审查分支的结果合并因此无需手写。编排层能力与业务需求一一对应：条件边实现自愈回退、fan-out/fan-in 实现三 Agent 并行、checkpointer 提供可审计的状态快照与断点恢复（直接兑现"可审计原则"）、`graph.stream()` 的节点级事件直通 API Server 的 SSE 实时进度。分工边界：LangGraph 负责"流程编排"，AI SDK 负责"单步 LLM 调用与工具循环"，互不重叠。

**分析引擎层**：包含四类分析引擎——LLM Agent（基于 AI SDK 5 的 ReAct 循环语义审查）、静态分析器（Tree-sitter 多语言 AST 解析 + ts-morph/oxc 的 JS/TS 深度分析 + Semgrep 规则扫描）、RAG 知识库（LanceDB 嵌入式向量检索 + MiniSearch BM25 混合召回）、测试沙箱（Docker 隔离的验证环境，Phase 4）。工具默认以进程内函数调用（In-Process Tool Bus）暴露，性能最优；同时通过 MCP 协议（stdio/SSE）对外暴露同一套工具，供第三方 Agent（如 Claude Code）即插即用。

**数据层**：SQLite（默认）/ PostgreSQL（团队版）经 Drizzle ORM 双方言同构访问，存储审查记录和配置；LanceDB 为**嵌入式**向量数据库（npm 原生绑定，随应用进程运行，无需独立服务），存储代码语义索引；Redis 用于团队版的缓存和任务队列（单机版以 SQLite 表降级实现）；文件系统存储 Markdown/HTML 报告；prom-client 暴露 Prometheus 指标。

#### 2.2 Monorepo 工程结构（pnpm 10 Workspaces + Turborepo）

```
ai-review/
├── apps/
│   ├── cli/                 # @ai-review/cli —— Git Hook 与命令行入口（commander + @clack/prompts）
│   │   └── src/commands/    #   init / run / index / install-hook / server
│   ├── server/              # @ai-review/server —— Hono 4 API：REST + SSE + 托管 web 静态产物
│   └── web/                 # @ai-review/web —— React 19 Dashboard（Vite 8 构建）
├── packages/
│   ├── shared/              # @ai-review/shared —— zod schema 与类型：配置/API/LLM 输出单一事实源
│   ├── diff/                # @ai-review/diff —— unified diff 解析、index 内容读取、上下文包构建
│   ├── core/                # @ai-review/core —— LangGraph.js 编排、风险规划器、流水线装配
│   ├── agents/              # @ai-review/agents —— ReAct 循环与三大审查 Agent
│   ├── tools/               # @ai-review/tools —— 工具总线与 MCP Server（AST/复杂度/密钥/依赖）
│   ├── rag/                 # @ai-review/rag —— LanceDB 向量索引 + MiniSearch 混合检索
│   ├── llm/                 # @ai-review/llm —— AI SDK Provider 封装、降级链、token 预算器
│   ├── db/                  # @ai-review/db —— Drizzle schema 与迁移（SQLite/PG 双方言）
│   └── report/              # @ai-review/report —— Markdown/HTML/JSON 报告生成
├── tooling/
│   ├── eslint-config/       # 共享 ESLint 9 flat config + oxlint 规则
│   └── tsconfig/            # 共享 tsconfig 基线（TypeScript 6.x, strict）
├── pnpm-workspace.yaml      # pnpm 10 workspaces + catalog 统一依赖版本
├── turbo.json               # Turborepo 任务图：build / typecheck / test / dev
└── package.json             # packageManager 字段锁定 pnpm 版本（corepack 激活）
```

**工程约定**：

- **依赖版本集中管理**：所有第三方依赖版本统一声明在 `pnpm-workspace.yaml` 的 `catalog` 中，各包以 `catalog:` 协议引用，杜绝多包版本漂移。

```yaml
# pnpm-workspace.yaml
packages:
  - 'apps/*'
  - 'packages/*'
  - 'tooling/*'

catalog:
  typescript: ^6.0.0 # TS 6.x 为 JS 实现的最终版本线，可跟进 TS 7 原生编译器（tsgo）
  react: ^19.2.0
  react-dom: ^19.2.0
  vite: ^8.0.0 # Vite 8：Rolldown 为唯一打包内核（2026-03 GA）
  zod: ^4.0.0
  hono: ^4.9.0
  ai: ^5.0.0 # Vercel AI SDK 5
  '@langchain/langgraph': ^1.0.0 # LangGraph.js（Agent 编排引擎）
  drizzle-orm: latest
```

- **构建分工**：`apps/web` 使用 Vite 8（开发期 Rolldown 驱动即时 HMR，生产构建由 Rolldown 一并替代 esbuild + Rollup，构建速度提升 10 倍以上）；`apps/cli` 与 `packages/*` 使用 tsdown（同为 Rolldown 内核的库/CLI 打包器）产出 ESM + d.ts。
- **任务编排**：Turborepo 按依赖图调度，`typecheck`/`test` 依赖上游包先完成 `build`；CI 中启用 Turborepo 远程缓存加速。

```json
// turbo.json
{
  "tasks": {
    "build": { "dependsOn": ["^build"], "outputs": ["dist/**"] },
    "typecheck": { "dependsOn": ["^build"] },
    "test": { "dependsOn": ["^build"] },
    "lint": {},
    "dev": { "cache": false, "persistent": true }
  }
}
```

- **根 package.json 锁定工具链**：

```json
{
  "name": "ai-review-monorepo",
  "private": true,
  "packageManager": "pnpm@10.17.0",
  "engines": { "node": ">=22" },
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "test": "turbo test",
    "typecheck": "turbo typecheck",
    "lint": "turbo lint"
  }
}
```

#### 2.3 端到端类型安全链路

以 `packages/shared` 的 zod schema 为单一事实源，类型沿四条链路自动传播：

```
zod Schema（shared 包）
   ├── 配置校验：loadConfig() 解析 .ai-review.yml，字段级错误提示
   ├── API 契约：Hono 路由 zValidator 入参校验 + 类型化响应（可导出 OpenAPI 文档）
   ├── 数据库：Drizzle 表定义与 schema 类型对齐，迁移前 typecheck 把关
   └── LLM 输出：审查发现的 JSON 输出经 zod 解析，非法字段直接触发自愈回退
        └── 前端：web 包直接 import shared 的类型与校验器，表单/列表零手工类型
```

### 三、核心模块详细设计

#### 3.0 核心业务链路总览（已确认）

审查流水线的完整业务链路如下，本方案对其逐环节做了确认与修正（关键修正见括号标注）：

1. **触发**：开发者执行 `git commit` → pre-commit hook 调用 `ai-review run --staged`（默认 **fast 模式**，控制提交等待时间；原方案无快慢模式区分，全量审查易阻塞提交体验）。
2. **Diff 解析**：读取**暂存区（index）快照**而非工作区文件——pre-commit 阶段工作区可能包含未暂存改动，只有 index 内容才与本次 commit 一致（**关键正确性修正**，原方案未明确数据源）；基于 diff 构建函数级上下文包。
3. **风险规划**：Risk Planner 对每个变更文件评分分流：≥60 深度 LLM 审查、40~60 快速 LLM 审查、<40 仅静态分析。
4. **并行审查**：正确性、安全、性能三个审查 Agent 经 LangGraph fan-out 三路并行执行，ReAct 循环中按需调用工具，全程受 token 预算器与取消信号约束。
5. **交叉验证**：5 阶段后处理（严重度重分类 → 置信度评分 → 跨文件分析 → 自动修复建议 → 去重排序）抑制幻觉。
6. **自愈回退**：BLOCKER 级发现置信度 < 0.6 时，携带上下文重审，最多 3 轮、逐轮收窄范围。
7. **报告与决策**：生成五段式 Markdown/JSON 报告，按 `block_on` 阈值决定退出码（0 放行 / 1 阻断提交）。
8. **落库与推送**：结果经缓存层（内容哈希去重，避免重复审查相同代码块）写入 SQLite，并通过 SSE 实时推送 Dashboard；CI 模式下自动发表 PR 评论。

**fast / full 双模式定义**（确认的核心业务参数）：

| 环节         | fast（pre-commit 默认）                | full（CI / 手动 `--mode full`） |
| ------------ | -------------------------------------- | ------------------------------- |
| 静态分析     | 全部执行（确定性、低开销）             | 全部执行                        |
| LLM 深度审查 | 仅风险分 ≥60 文件，正确性 + 安全两维度 | 所有 deep 文件，四维度全开      |
| RAG 检索     | 仅高风险文件注入                       | 全部文件注入                    |
| 交叉验证     | 简化（去重 + 置信度过滤）              | 完整 5 阶段 + 3 轮自愈回退      |
| 目标耗时     | ≤ 15s（500 行变更）                    | ≤ 60s（500 行变更）             |

#### 3.1 Diff 解析与上下文提取模块

这是整个审查流水线的入口。Git diff 提供了行级别的变更信息，但缺少语义上下文。本模块在 Git 原生 diff 的基础上，构建结构化的、AI 就绪的代码上下文包。

```typescript
// packages/diff/src/context-builder.ts
import { execFile } from 'node:child_process';
import { promisify } from 'node:util';
import { pMap } from 'p-map';
import { parseUnifiedDiff } from './parser'; // unified diff 解析（封装 parse-git-diff）
import { extractEnclosingFunctions, extractAstSignature } from './ast'; // Tree-sitter
import { isMeaningfulChange, classifyChange } from './filter';
import type {
  CodeContext,
  FileContext,
  FileDiff,
  GitRunner,
  RagRetriever,
  IgnoreRules,
} from '@ai-review/shared';

const exec = promisify(execFile);

export class DiffContextBuilder {
  constructor(
    private readonly repoPath: string,
    private readonly rag: RagRetriever,
    private readonly ignores: IgnoreRules,
    private readonly contextRadius: number = 50,
  ) {}

  /**
   * 核心正确性约定：审查对象是暂存区（index）快照，而不是工作区。
   * pre-commit 阶段工作区可能包含未暂存改动，只有 index 内容才与本次 commit 一致。
   * 采用 git show :path 只读 index blob，不使用 git stash --keep-index 等会改动工作区的方案。
   */
  async build(): Promise<CodeContext> {
    const git: GitRunner = async (...args: string[]) =>
      (await exec('git', args, { cwd: this.repoPath, maxBuffer: 64 * 1024 * 1024 })).stdout;

    // 1. 读取暂存区 diff，扩大 unified 上下文到 ±N 行，便于后续函数级理解
    const stagedDiff = await git('diff', '--cached', `--unified=${this.contextRadius}`);

    // 2. 解析 unified diff，提取变更文件与行范围
    const fileDiffs: FileDiff[] = parseUnifiedDiff(stagedDiff);

    // 3. 过滤：二进制文件、纯空白变更、命中 ignore 规则的文件（锁文件/构建产物等）
    const meaningful = fileDiffs.filter(
      (d) => !d.binary && this.ignores.allows(d.path) && isMeaningfulChange(d),
    );

    // 4. 按文件构建上下文包（I/O 并行，限流防止大提交打爆 git 进程）
    const contexts = await pMap(meaningful, (d) => this.buildFileContext(d, git), {
      concurrency: 8,
    });

    return { files: contexts, metadata: this.buildMetadata(contexts) };
  }

  private async buildFileContext(diff: FileDiff, git: GitRunner): Promise<FileContext> {
    // 4a. 从 index 读取文件完整内容（新增文件 index 中已有 blob；删除文件降级为仅 diff 审查）
    const stagedContent = await git('show', `:${diff.path}`).catch(() => '');

    // 4b. Tree-sitter 定位变更行所在函数/类，提取完整源码与 AST 签名（非仅 diff 行）
    const snippet = extractEnclosingFunctions(stagedContent, diff.changedLines, this.contextRadius);
    const signature = extractAstSignature(snippet);

    // 4c. RAG 检索：以变更摘要为查询，召回相似实现、项目约定与历史修复记录
    const ragHits = await this.rag.query({
      query: diff.summary,
      topK: 5,
      filter: { path: diff.path },
    });

    return { diff, snippet, signature, ragHits, changeType: classifyChange(diff) };
  }
}
```

**关键设计决策**：

- **以 index 为唯一事实源**：`git show :path` 直接读取暂存区 blob，保证"审查的内容 = 提交的内容"，杜绝工作区未暂存改动混入审查造成的误判。
- **上下文半径可配置**：默认提取变更行前后 50 行的代码，足以覆盖大多数函数的完整逻辑，同时控制 token 消耗；Tree-sitter 的函数边界裁剪进一步去除无关代码。
- **忽略删除行**：AI 审查聚焦于新增/修改的代码；删除操作由确定性工具处理（如检测是否删除了必要的错误处理——该信号同时供 Risk Planner 加权）。
- **RAG 检索以变更摘要为查询**：利用 diff 的变更摘要（由小模型快速生成或从 commit message 提取）作为向量检索的查询，召回相关的历史修复和项目约定。

#### 3.2 风险规划器（Risk Planner）

在 LLM 审查之前，风险规划器对变更文件进行优先级排序，将 LLM 的注意力集中在高风险区域，减少 token 浪费。这是确定性规划器（Deterministic Planner）模式的具体实现。

**风险评估规则**：

| 维度     | 低风险               | 中风险             | 高风险                    |
| -------- | -------------------- | ------------------ | ------------------------- |
| 文件类型 | 测试文件、文档       | 工具函数、配置文件 | 认证/授权、支付、数据访问 |
| 变更规模 | < 10 行              | 10-100 行          | > 100 行                  |
| 变更类型 | 格式调整             | 新功能             | 逻辑修改、删除错误处理    |
| 文件历史 | 稳定（> 6 月无修改） | 偶发修改           | 高频修改（> 5 次/月）     |

每个文件获得 0-100 的风险分数，分数 ≥ 60 的文件进入深度 LLM 审查，40-60 的文件进入快速审查，< 40 的文件仅运行静态分析。

```typescript
// packages/core/src/risk-planner.ts
import type { FileDiff, FileHistory } from '@ai-review/shared';

/** 高风险路径特征：认证、支付、数据访问等（大小写不敏感） */
const HIGH_RISK_PATTERNS: RegExp[] = [
  /auth/,
  /login/,
  /password/,
  /token/,
  /secret/,
  /permission/,
  /payment/,
  /billing/,
  /admin/,
  /sql/,
  /query/,
  /execute/,
  /eval/,
];

/** 删除行中的高风险信号：移除错误处理/校验是典型危险变更 */
const REMOVED_GUARD_PATTERNS: RegExp[] = [
  /\b(try|except|catch|finally|rollback)\b/,
  /\b(validate|sanitize|escape|checkPermission|requireUser)\b/,
];

export interface ScoredFile {
  diff: FileDiff;
  score: number;
}
export interface ReviewPlan {
  deep: ScoredFile[];
  quick: ScoredFile[];
  staticOnly: ScoredFile[];
}

export class RiskPlanner {
  constructor(private readonly history: FileHistory) {}

  async plan(diffs: FileDiff[]): Promise<ReviewPlan> {
    const scored: ScoredFile[] = [];

    for (const diff of diffs) {
      let score = 0;
      const path = diff.path.toLowerCase();

      // ① 路径风险：命中认证/支付等敏感路径 +30
      if (HIGH_RISK_PATTERNS.some((p) => p.test(path))) score += 30;

      // ② 变更规模
      const changed = diff.additions + diff.deletions;
      if (changed > 100) score += 25;
      else if (changed > 30) score += 15;

      // ③ 删除守卫逻辑 +20（删除错误处理是高风险信号）
      if (diff.removedLines.some((line) => REMOVED_GUARD_PATTERNS.test(line))) score += 20;

      // ④ 文件热度：近 30 天修改次数 > 5 +15
      if ((await this.history.changeFrequency(diff.path, 30)) > 5) score += 15;

      // ⑤ 测试/文档/纯格式化降权
      if (isTestOrDocs(path) || isFormattingOnly(diff)) score = Math.max(0, score - 25);

      scored.push({ diff, score: Math.min(score, 100) });
    }

    scored.sort((a, b) => b.score - a.score);
    return {
      deep: scored.filter((s) => s.score >= 60), // 深度 LLM 审查（全维度）
      quick: scored.filter((s) => s.score >= 40 && s.score < 60), // 快速 LLM 审查（单维度）
      staticOnly: scored.filter((s) => s.score < 40), // 仅静态分析，不消耗 LLM token
    };
  }
}
```

#### 3.3 多 Agent 并行审查引擎

审查引擎采用多 Agent 并行架构，三个审查 Agent 分别从不同维度独立分析代码，避免"三个 Reviewer 审同一批代码——都审不深"的问题。

**Agent 1：正确性与逻辑审查 Agent**

职责：检测逻辑错误、边界条件缺失、异常处理遗漏、并发安全问题。

System Prompt 设计要点：

```
你是一位资深代码审查专家，专注于代码正确性和逻辑缺陷检测。

审查规则：
- 检查边界条件：空值、越界、除零、类型转换异常
- 检查异常处理：是否正确捕获并处理异常，是否存在静默失败
- 检查并发安全：共享状态是否有竞态条件，锁的粒度是否合理
- 检查资源管理：文件/连接/内存是否正确释放
- 检查返回值：所有代码路径是否都有合理的返回值

输出格式：每个问题必须包含 [严重度] [文件:行号] [问题描述] [修复建议]
严重度分级：BLOCKER / WARNING / NIT / PRAISE

禁止行为：
- 不要对不确定的问题编造具体的修复代码
- 不要报告纯风格问题（由其他 Agent 处理）
- 如果无法确定问题是否真实存在，标记为 LOW_CONFIDENCE
```

**Agent 2：安全与风险审查 Agent**

职责：检测安全漏洞、敏感信息泄露、依赖风险、权限问题。

覆盖的漏洞类型参考 CWE 分类，重点覆盖注入类（CWE-89/79）、认证授权类（CWE-287/862）、敏感数据暴露（CWE-200/798）、不安全反序列化（CWE-502）等。

**Agent 3：性能与可维护性审查 Agent**

职责：检测性能反模式、代码复杂度、可维护性问题。

检测项包括：N+1 查询、不必要的循环嵌套、缺失索引提示、过长函数（> 50 行）、过深嵌套（> 4 层）、魔法数字、重复代码块。

**并行执行与资源约束**：

```typescript
// packages/core/src/nodes/review-nodes.ts
import pLimit from 'p-limit';
import { correctAgent, securityAgent, perfAgent } from '@ai-review/agents';
import type { ReviewAgent } from '@ai-review/agents';
import type { ReviewState } from '@ai-review/shared';

const fileGate = pLimit(8); // 单 Agent 内文件级并发限流

// LangGraph fan-out 机制会将三个节点并行调度，无需手写 Promise.all；
// 各节点只返回增量结果，合并去重交给 findings channel 的 reducer（见 3.9）
const makeReviewNode =
  (agent: ReviewAgent) =>
  async (state: ReviewState): Promise<Partial<ReviewState>> => ({
    findings: await agent.review(state, fileGate),
  });

export const correctnessNode = makeReviewNode(correctAgent);
export const securityNode = makeReviewNode(securityAgent);
export const performanceNode = makeReviewNode(perfAgent);
```

**Token 预算器**：全流水线共享一个预算实例，耗尽时未审查的低优先级文件自动降级为 `staticOnly`，并在报告中标注"预算受限，已降级"，保证审查成本有硬上限：

```typescript
// packages/llm/src/budget.ts
export class TokenBudget {
  private used = 0;
  constructor(
    private readonly max: number,
    private readonly onExhausted?: () => void,
  ) {}

  tryReserve(estimate: number): boolean {
    if (this.used + estimate > this.max) {
      this.onExhausted?.();
      return false;
    }
    return true;
  }

  account(usage: { totalTokens: number }): void {
    this.used += usage.totalTokens;
  }
  get usageRatio(): number {
    return this.used / this.max;
  }
}
```

#### 3.4 静态分析集成层

静态分析层通过工具总线（默认进程内调用，对外经 MCP 协议暴露）供 Agent 在 ReAct 循环中按需调用。

**核心工具清单**：

| 工具               | 功能         | 输入      | 输出                    |
| ------------------ | ------------ | --------- | ----------------------- |
| `ast_parse`        | AST 解析     | 文件路径  | 函数/类列表、复杂度指标 |
| `complexity_check` | 圈复杂度计算 | 文件路径  | 超标函数列表            |
| `dependency_scan`  | 依赖漏洞扫描 | 依赖文件  | CVE 列表 + 严重度       |
| `secret_scan`      | 敏感信息检测 | diff 文本 | 疑似密钥/Token 列表     |
| `test_impact`      | 测试影响分析 | 变更文件  | 受影响的测试用例列表    |
| `call_graph`       | 调用图构建   | 函数名    | 调用链和依赖关系        |

**JS/TS 圈复杂度分析**（ts-morph 实现，与本项目技术栈同构；Python/Go/Java 走 Tree-sitter 语法包，JS/TS 亦可切换 oxc-parser 获得数量级解析提速）：

```typescript
// packages/tools/src/complexity.ts
import { Project, Node, SyntaxKind } from 'ts-morph';

export interface FunctionMetrics {
  name: string;
  line: number;
  complexity: number; // 1 + 分支/循环/异常/短路表达式节点数
  params: string[];
  isAsync: boolean;
}

const DECISION_KINDS = [
  SyntaxKind.IfStatement,
  SyntaxKind.ForStatement,
  SyntaxKind.ForOfStatement,
  SyntaxKind.ForInStatement,
  SyntaxKind.WhileStatement,
  SyntaxKind.DoStatement,
  SyntaxKind.CatchClause,
  SyntaxKind.CaseClause,
  SyntaxKind.ConditionalExpression,
];

export function analyzeComplexity(source: string, threshold = 15): FunctionMetrics[] {
  const project = new Project({ useInMemoryFileSystem: true });
  const file = project.createSourceFile('reviewed.ts', source);
  const functions = [
    ...file.getDescendantsOfKind(SyntaxKind.FunctionDeclaration),
    ...file.getDescendantsOfKind(SyntaxKind.MethodDeclaration),
    ...file.getDescendantsOfKind(SyntaxKind.ArrowFunction),
  ];

  return functions
    .map((fn) => {
      let complexity = 1;
      for (const node of fn.getDescendants()) {
        if (DECISION_KINDS.includes(node.getKind())) complexity++;
        if (Node.isBinaryExpression(node)) {
          const op = node.getOperatorToken().getKind();
          if (
            op === SyntaxKind.AmpersandAmpersandToken ||
            op === SyntaxKind.BarBarToken ||
            op === SyntaxKind.QuestionQuestionToken
          )
            complexity++;
        }
      }
      return {
        name: fn.getName() ?? '(anonymous)',
        line: fn.getStartLineNumber(),
        complexity,
        params: fn.getParameters().map((p) => p.getName()),
        isAsync: fn.isAsync(),
      };
    })
    .filter((m) => m.complexity >= threshold);
}
```

**MCP 工具服务器的对外暴露**（同一套工具函数，进程内与 MCP 双通道复用）：

```typescript
// packages/tools/src/mcp-server.ts
import { McpServer } from '@modelcontextprotocol/sdk/server/mcp.js';
import { StdioServerTransport } from '@modelcontextprotocol/sdk/server/stdio.js';
import { z } from 'zod';

const server = new McpServer({ name: 'ai-review-tools', version: '1.0.0' });

server.registerTool(
  'complexity_check',
  {
    title: '圈复杂度检查',
    description: '计算 TS/JS 源码中超过阈值的函数列表',
    inputSchema: { file_path: z.string(), threshold: z.number().optional() },
  },
  async ({ file_path, threshold }) => {
    const result = analyzeComplexity(readFileText(file_path), threshold ?? 15);
    return { content: [{ type: 'text', text: JSON.stringify(result) }] };
  },
);

// secret_scan / dependency_scan / test_impact / call_graph 同理注册
await server.connect(new StdioServerTransport());
```

#### 3.5 RAG 知识库模块

RAG 知识库为 LLM 审查提供项目上下文，有效提升业务逻辑类问题的召回率。根据 Google SECL 基准测试，约 2000 token 的项目背景信息可使逻辑漏洞召回率提升约 2 倍。

**知识库构建流程**：

1. **代码索引**：基于 Tree-sitter 在函数/类边界切分代码，使用 AI SDK 的 `embedMany`（云端 `text-embedding-3-large` 或本地 Ollama `nomic-embed-text`）生成向量，存入 LanceDB（嵌入式，随项目 `.ai-review-cache/` 目录持久化，零外部服务依赖）。
2. **项目文档索引**：将 `README.md`、`CLAUDE.md`、架构文档、API 规范等向量化存储。
3. **历史记录索引**：将历史审查报告、修复 commit 的 message 向量化，供检索相似问题的历史处理方式。

**检索策略**：采用 BM25（MiniSearch 内存倒排索引）+ 余弦相似度的混合检索，通过倒数排名融合（RRF）合并结果。对于代码变更，检索三类上下文——同一文件的相似函数（理解编码风格）、项目中的相似模式（理解架构约定）、相关文档（理解业务规则）。

```typescript
// packages/rag/src/knowledge-base.ts
import * as lancedb from '@lancedb/lancedb';
import MiniSearch from 'minisearch';
import { embed, embedMany } from 'ai';
import type { EmbeddingModel } from 'ai';
import { chunkByAst } from './chunker';
import { rrfFuse } from './fusion';
import type { RagQuery, RagHit, Chunk } from '@ai-review/shared';

export class KnowledgeBase {
  private table?: lancedb.Table;
  private bm25 = new MiniSearch({ fields: ['text'], storeFields: ['id', 'path', 'kind'] });

  constructor(
    private readonly indexDir: string,
    private readonly embeddingModel: EmbeddingModel<string>,
  ) {}

  async build(paths: string[]): Promise<void> {
    // 1. 按 AST 边界切块（函数/类），代码/文档/历史报告分别入库
    const chunks: Chunk[] = await chunkByAst(paths);

    // 2. 批量向量化（AI SDK 自动分批与重试）
    const { embeddings } = await embedMany({
      model: this.embeddingModel,
      values: chunks.map((c) => c.text),
    });

    // 3. 写入 LanceDB（嵌入式，本地目录持久化）并同步构建 BM25 索引
    const db = await lancedb.connect(this.indexDir);
    this.table = await db.createTable(
      'chunks',
      chunks.map((c, i) => ({ ...c, vector: embeddings[i] })),
      { mode: 'overwrite' },
    );
    this.bm25.addAll(chunks.map((c) => ({ id: c.id, text: c.text, path: c.path, kind: c.kind })));
  }

  async query({ query, topK = 5, filter }: RagQuery): Promise<RagHit[]> {
    const [{ embedding: qvec }, bm25Hits] = await Promise.all([
      embed({ model: this.embeddingModel, value: query }),
      Promise.resolve(this.bm25.search(query).slice(0, topK * 3)),
    ]);

    const vecHits = await this.table!.search(qvec)
      .limit(topK * 3)
      .execute();

    // 倒数排名融合（RRF）：合并向量召回与 BM25 召回
    return rrfFuse(vecHits, bm25Hits, topK, filter);
  }
}
```

**增量索引**：以文件内容哈希为键做差量更新，仅在文件变更时重新切块与向量化；大型仓库首次索引耗时问题由此缓解。

#### 3.6 交叉验证与幻觉抑制模块

这是降低 LLM 幻觉率的关键模块，在 LLM 审查结束后进行 5 阶段后处理：

**阶段 1：严重度重分类**。使用规则数据库对 LLM 标记的严重度进行校正。例如，LLM 将测试文件中的硬编码标记为 BLOCKER，规则库会降级为 NIT。

**阶段 2：置信度评分**。每个发现获得 0-1 的置信度分数，基于：工具类型基础分（静态分析 0.9，LLM 语义 0.6）、是否有多源交叉确认（+0.2）、问题模式的已知误报率（调整）。

**阶段 3：跨文件分析**。检测 LLM 容易遗漏的跨文件问题——缺失测试覆盖（新函数无对应测试）、过时 import（引用了已删除的模块）、API 契约破坏（函数签名变更但调用方未更新）。本阶段大量借助 TypeScript 工具链能力：对 TS 项目直接用 ts-morph 做"签名变更 vs 调用方"的编译级核对，结论是确定性的。

**阶段 4：自动修复建议**。对匹配常见 linter 规则的问题（如未使用的 import、空 catch 块），复用 ESLint 规则元数据自动生成修复代码。

**阶段 5：去重与排序**。合并多个 Agent 的重复发现（按文件 + 行区间 + 问题类别聚类），按"严重度 × 置信度"排序。

**幻觉抑制策略**：

- **断言校验**：如果 LLM 声称某处存在 SQL 注入，但静态分析未检测到任何污点传播路径，则将置信度降为 LOW。
- **CVE 校验**：如果 LLM 引用了 CVE 编号，必须通过外部 API 验证其真实性，无法验证的 CVE 引用直接移除。
- **上下文约束**：在 System Prompt 中明确要求"不确定的问题标记为 LOW_CONFIDENCE 而非编造修复方案"。
- **结构化输出校验**：LLM 输出必须通过 zod schema 解析（文件存在、行号在 diff 范围内、字段类型正确），解析失败的发现整条丢弃并计入自愈触发条件。

#### 3.7 自愈回退机制

当交叉验证阶段发现高严重度问题的置信度低于阈值时，触发自愈回退——将问题及上下文反馈给 LLM Agent，请求重新审查。最多回退 3 轮，每轮回退时缩小审查范围（仅针对问题代码块）并提高 Prompt 的严格性。回退轮次由编排层条件边控制（见 3.9 流水线装配），每轮消耗独立计入 token 预算。

#### 3.8 报告生成模块

报告采用五段式结构，与 MCP Agent 的 PR 评审报告规范对齐：

```markdown
# 代码审查报告

## 元信息

- 审查 ID: review-20260911-001
- 提交者: developer@example.com
- 分支: feature/auth-refactor
- 审查模型: claude-sonnet-4.5 + 静态分析
- 风险评分: 72/100 (中高风险)

## 1. 变更概述 (Summary of Changes)

[AI 生成的变更摘要，包括改动意图、影响范围、变更类型]

## 2. 发现的问题 (Potential Issues and Bugs)

| 严重度  | 文件:行号   | 问题描述             | 置信度 | 修复建议        |
| ------- | ----------- | -------------------- | ------ | --------------- |
| BLOCKER | auth.py:142 | SQL 拼接导致注入风险 | HIGH   | 使用参数化查询  |
| WARNING | auth.py:87  | 缺失异常处理         | MEDIUM | 添加 try/except |

## 3. 代码质量考量 (Code Quality Considerations)

[复杂度分析、重复代码、命名规范]

## 4. 改进建议 (Suggested Improvements)

[可操作的改进项，含代码片段对比]

## 5. 总体评估 (Overall Assessment)

[是否建议提交/合并，以及理由]
```

报告由 `packages/report` 从 `ReviewReport` 类型化对象渲染，三种格式（Markdown/HTML/JSON）共享同一数据源；JSON 格式为 Dashboard 与 CI 评论提供数据接口。

#### 3.9 流水线装配（LangGraph.js 编排）

编排层采用 LangGraph.js（`@langchain/langgraph`）的 StateGraph：共享状态以 Channel/Reducer 声明，节点返回增量更新，条件边与并行分支由图结构表达，业务流程一目了然。

```typescript
// packages/core/src/state.ts —— 共享状态定义（Channel/Reducer 模型）
import { Annotation } from '@langchain/langgraph';
import type { CodeContext, Finding, ReviewMetrics, ReviewPlan } from '@ai-review/shared';

const FindingList = Annotation<Finding[]>({
  reducer: (current, patch) => dedupeFindings([...current, ...patch]), // 并行分支结果自动合并去重
  default: () => [],
});

export const ReviewState = Annotation.Root({
  context: Annotation<CodeContext>({ reducer: (_, b) => b, default: () => emptyContext() }),
  plan: Annotation<ReviewPlan>({ reducer: (_, b) => b, default: () => emptyPlan() }),
  findings: FindingList,
  healRounds: Annotation<number>({ reducer: (_, b) => b, default: () => 0 }),
  metrics: Annotation<ReviewMetrics>({ reducer: (_, b) => b, default: () => emptyMetrics() }),
});
```

```typescript
// packages/core/src/pipeline.ts —— LangGraph 流水线装配
import { StateGraph, START, END, MemorySaver } from '@langchain/langgraph';
// 团队版持久化（可审计/断点恢复）：
// import { SqliteSaver } from "@langchain/langgraph-checkpoint-sqlite";

function routeAfterValidate(s: ReviewState): 'heal' | typeof END {
  const needsHeal = s.findings.some((f) => f.severity === 'BLOCKER' && f.confidence < 0.6);
  return needsHeal && s.healRounds < 3 ? 'heal' : END;
}

export function buildPipeline() {
  return new StateGraph(ReviewState)
    .addNode('parse', parseDiffNode) // Diff 解析 + 上下文包构建
    .addNode('plan', riskPlanNode) // 风险规划分流
    .addNode('correctness', correctnessNode) // 三条并行审查分支
    .addNode('security', securityNode)
    .addNode('performance', performanceNode)
    .addNode('validate', crossValidateNode) // 交叉验证 + 幻觉抑制
    .addNode('heal', selfHealNode) // 自愈回退（条件进入）
    .addNode('report', reportNode) // 报告组装、落库、推送
    .addEdge(START, 'parse')
    .addEdge('parse', 'plan')
    .addEdge('plan', ['correctness', 'security', 'performance']) // fan-out：三路并行
    .addEdge('correctness', 'validate') // fan-in：汇聚点等待全部分支完成
    .addEdge('security', 'validate')
    .addEdge('performance', 'validate')
    .addConditionalEdges('validate', routeAfterValidate)
    .addEdge('heal', 'validate')
    .addEdge('report', END)
    .compile({ checkpointer: new MemorySaver() }); // 开发用内存快照，团队版换 SQLite/Postgres Saver
}

// 执行：thread_id = 审查 ID；每个 superstep 后 checkpoint 保存一次状态快照
const app = buildPipeline();
for await (const update of app.stream(initialState, {
  configurable: { thread_id: reviewId },
  streamMode: 'updates', // 节点级增量事件
})) {
  publishToSSE(reviewId, update); // 直通 API Server 的 SSE → Dashboard 实时进度
}
```

**编排层设计要点**：

- **并行审查即 fan-out/fan-in**：`plan → [correctness, security, performance]` 三路并行由图结构表达，LangGraph 的 superstep 模型保证汇聚点等待全部分支完成；发现结果经 `findings` reducer 自动合并去重，无需手写 Promise.all。
- **条件边 = 自愈回退**：`routeAfterValidate` 精确对应 3.0 的业务规则（BLOCKER 置信度 < 0.6 且 healRounds < 3 → heal），业务规则与图结构一一对应。
- **checkpoint 可审计**：checkpointer 在每个节点后保存状态快照，审查中断可从断点恢复、完整执行轨迹可回放，直接兑现"可审计原则"；团队版切换 SqliteSaver/PostgresSaver 即可持久化。
- **流式事件直通 Dashboard**：`graph.stream()` 的节点级更新经 SSE 透传，3.10 的实时进度条无需额外事件总线。
- **人机协同扩展点**：LangGraph 的 `interrupt()` 可在关键节点（如 BLOCKER 处置）暂停等待人工决策，为 Phase 4 的"人工复核工作流"预留能力。
- **全局取消**：取消信号经 `app.invoke(..., { signal })` 传入并传播到所有运行中节点，Ctrl-C 立即中断所有 LLM 请求。

#### 3.10 Web Dashboard 前端设计（React 19 + Vite 8）

Dashboard 是团队版的审查结果消费端，技术选型与页面设计如下。

**前端技术栈**：

| 关注点     | 选型                           | 说明                                                                         |
| ---------- | ------------------------------ | ---------------------------------------------------------------------------- |
| 框架       | React 19                       | 主流版本；配合 React Compiler 自动 memoization，消除手写 useMemo/useCallback |
| 构建       | Vite 8（Rolldown 内核）        | Rust 打包核心统一 dev/build，冷启动与热更新数量级提速                        |
| 路由       | TanStack Router                | 全类型安全路由（参数、search params 全链路类型推导），文件式路由             |
| 服务端状态 | TanStack Query v5              | 请求缓存、失效、重试；SSE 用自定义 hook 接入                                 |
| UI 组件    | Ant Design 6                   | 原生支持 React 19（无需 v5 兼容补丁）；备选 shadcn/ui + Tailwind CSS 4       |
| 图表       | Apache ECharts 6               | 审查趋势、风险分布、token 成本曲线（按需注册控制体积）                       |
| Diff 渲染  | react-diff-view + shiki        | 行内严重度标注 + 语法高亮                                                    |
| 运行时校验 | zod（复用 @ai-review/shared）  | 与后端共享 API 契约，零手工类型                                              |
| 测试       | Vitest + Testing Library + MSW | MSW 拦截 API 做组件测试与 Storybook mock                                     |

**页面设计**：

1. **审查历史**：列表 + 多维筛选（状态/严重度/作者/时间范围），行内展示风险分与 BLOCKER 计数，点击进入报告详情。
2. **报告详情**：左侧 diff 视图（unified/split 切换，shiki 高亮），发现项以行内色条 + 悬浮卡片标注（严重度、置信度、修复建议）；右侧发现列表支持**标记误报**（`useOptimistic` 乐观更新，回写数据库——同时为后续强化学习优化收集反馈数据）。
3. **实时审查进度**：通过 SSE 订阅运行中的审查，阶段进度条（解析 → 规划 → 审查 → 验证 → 报告）实时刷新。
4. **统计分析**：ECharts 展示风险分趋势、检出率、token 成本随时间变化、Top 高风险文件、各 Agent 命中率。
5. **知识库与配置管理**：RAG 索引状态与重建、`.ai-review.yml` 可视化编辑（zod 校验实时预览）。

**构建配置**：

```typescript
// apps/web/vite.config.ts
import { defineConfig } from 'vite';
import react from '@vitejs/plugin-react';

export default defineConfig({
  // Vite 8 默认即 Rolldown 内核，无需额外 bundler 配置
  plugins: [
    react({
      babel: { plugins: [['babel-plugin-react-compiler', {}]] }, // React Compiler 自动 memo
    }),
  ],
  server: { proxy: { '/api': 'http://localhost:8080' } }, // dev 期转发到 Hono
  build: {
    rollupOptions: {
      output: { manualChunks: { echarts: ['echarts'], antd: ['antd'] } }, // 大依赖独立分包
    },
  },
});
```

**部署形态**：Dashboard 构建为纯静态产物（SPA），由 Hono 服务端直接托管（`serveStatic`），单容器即可同时提供 API 与前端，内网部署零额外设施。选择 Vite SPA 而非 Next.js 的原因：Dashboard 为内网工具，无 SEO/SSR 需求，SPA + API 托管的部署拓扑最简，且与 monorepo 的 Rolldown 工具链完全统一。

#### 3.11 CLI 设计

CLI 是个人开发者的主入口，强调低摩擦与可脚本化：

| 命令                            | 说明                                                           |
| ------------------------------- | -------------------------------------------------------------- |
| `ai-review init`                | 交互式向导（@clack/prompts）生成 `.ai-review.yml` 与知识库骨架 |
| `ai-review run --staged`        | 审查暂存区变更（pre-commit 默认入口，fast 模式）               |
| `ai-review run --base <ref>`    | CI 模式：审查 `<ref>...HEAD` 区间变更（full 模式）             |
| `ai-review run --mode full`     | 本地手动触发全量深度审查                                       |
| `ai-review index --paths <...>` | 构建/增量更新 RAG 向量索引                                     |
| `ai-review install-hook`        | 写入 `.husky/pre-commit`（husky v9，Windows/macOS/Linux 通用） |
| `ai-review server`              | 启动 API Server + Dashboard（团队版）                          |

```typescript
// apps/cli/src/index.ts
import { Command } from 'commander';
import { runReview } from './commands/run';
import { initConfig } from './commands/init';
import { buildIndex } from './commands/index';
import { installHook } from './commands/hook';

const program = new Command();

program.name('ai-review').description('AI Code Review Agent for Vibe Coding').version('1.0.0');

program
  .command('run')
  .description('审查暂存区或区间变更')
  .option('--staged', '审查暂存区 diff（默认）')
  .option('--base <ref>', 'CI 模式：审查 <ref> 到 HEAD 的变更')
  .option('--mode <mode>', 'fast | full', 'fast')
  .option('--block-on <severity>', '阻断阈值', 'BLOCKER')
  .option('--json', '以 JSON 输出（供 CI/脚本消费）')
  .action(runReview);

program.command('init').description('交互式生成配置与知识库骨架').action(initConfig);
program
  .command('index')
  .description('构建/增量更新 RAG 索引')
  .option('--paths <paths...>')
  .action(buildIndex);
program.command('install-hook').description('安装 pre-commit hook').action(installHook);

await program.parseAsync();
```

**退出码契约**（脚本与 CI 依赖的稳定接口）：

| 退出码 | 含义                                                            |
| ------ | --------------------------------------------------------------- |
| 0      | 审查通过（或无发现超过阻断阈值）                                |
| 1      | 存在 ≥ blockOn 级别的发现，阻断提交                             |
| 2      | 配置/环境错误（配置校验失败、git 仓库无效等）                   |
| 3      | 审查超时或被取消（不阻断提交，终端提示可用 `--mode full` 重试） |

### 四、技术栈选型

| 层次           | 技术选型                                                                   | 版本基线                     | 理由                                                                                                      |
| -------------- | -------------------------------------------------------------------------- | ---------------------------- | --------------------------------------------------------------------------------------------------------- |
| 编程语言       | TypeScript（前后端统一）                                                   | 6.x（strict）                | 全栈单语言；TS 6.0 为 JS 实现最终版，可跟进 TS 7 原生编译器（tsgo，Go 实现，编译提速约 10 倍）            |
| 运行时         | Node.js                                                                    | 22 LTS（24 LTS 可选）        | CLI/服务端同构；内置 fetch 等现代能力                                                                     |
| 包管理         | pnpm                                                                       | 10.x（workspaces + catalog） | 硬链接节省磁盘、严格依赖隔离、catalog 集中锁版本                                                          |
| Monorepo 编排  | Turborepo                                                                  | 2.x                          | 任务图调度 + 远程缓存                                                                                     |
| 后端框架       | Hono                                                                       | 4.x                          | Web 标准优先、原生 SSE 流式、zod 中间件生态、轻量可移植                                                   |
| 前端框架       | React                                                                      | 19.x                         | 主流生态 + React Compiler 自动 memoization                                                                |
| 前端构建       | Vite 8（Rolldown 内核）                                                    | 8.x                          | Rolldown（Rust）统一 dev/build 打包，替代 esbuild+Rollup，构建提速 10 倍以上                              |
| CLI/库构建     | tsdown（Rolldown 内核）                                                    | latest                       | packages 与 CLI 的 ESM + d.ts 产物，与 Vite 8 共享 Rolldown 工具链                                        |
| 前端路由/状态  | TanStack Router / Query                                                    | v1 / v5                      | 全类型安全路由；服务端状态缓存与失效                                                                      |
| UI 组件库      | Ant Design                                                                 | 6.x（原生支持 React 19）     | 企业级组件完备；备选 shadcn/ui + Tailwind CSS 4                                                           |
| 可视化         | Apache ECharts                                                             | 6.x                          | 审查统计图表                                                                                              |
| LLM 接入       | Vercel AI SDK                                                              | 5.x                          | 统一 anthropic/openai/ollama 三类 Provider，内置工具调用循环、流式输出、usage 统计与重试                  |
| Agent 编排     | LangGraph.js（@langchain/langgraph）                                       | 1.x                          | 官方 1.0 稳定版：条件边、fan-out/fan-in 并行、checkpoint 持久化、节点级流式事件、interrupt 人机协同扩展点 |
| 工具协议       | MCP TypeScript SDK                                                         | 1.x                          | 分析工具对内进程调用、对外 MCP 即插即用                                                                   |
| AST/静态分析   | Tree-sitter（多语言）+ ts-morph / oxc-parser（JS/TS）+ Semgrep（外部进程） | —                            | 语义切分、复杂度、安全规则多语言覆盖                                                                      |
| 依赖与密钥扫描 | 内置规则 + gitleaks（可选外部二进制）                                      | —                            | 高置信正则规则先行，重规则外部工具兜底                                                                    |
| 向量数据库     | LanceDB（嵌入式，`@lancedb/lancedb`）                                      | —                            | 零服务依赖、本地优先，随项目目录持久化；备选 sqlite-vec                                                   |
| BM25 检索      | MiniSearch                                                                 | latest                       | 轻量内存倒排索引，与向量检索做 RRF 融合                                                                   |
| ORM / 数据库   | Drizzle ORM + better-sqlite3 / node-postgres                               | —                            | 双方言同构 schema，零运行时开销，迁移用 drizzle-kit                                                       |
| 数据校验       | zod                                                                        | 4.x                          | 配置/API/LLM 输出统一校验，前后端共享                                                                     |
| 测试           | Vitest + Testing Library + Playwright                                      | —                            | 与 Vite 8 同源配置；Playwright 负责 Dashboard E2E 冒烟                                                    |
| 代码质量       | ESLint 9 + oxlint + Prettier；husky 9 + lint-staged                        | —                            | oxlint（Rust）毫秒级兜底扫描                                                                              |
| 日志/指标      | pino；prom-client + Grafana                                                | —                            | 结构化日志；审查耗时/token 消耗/检出率可视化                                                              |
| 部署           | Docker multi-stage（node:22-alpine + corepack pnpm）                       | —                            | 单容器托管 API + 静态 Dashboard                                                                           |

**选型补充说明**：

- **为什么 AI SDK 而非 LangChain**：本项目 Agent 拓扑固定、工具数量有限，AI SDK 5 已原生覆盖多 Provider 抽象、工具调用循环（`stopWhen` 控制步数）、流式与 usage 统计；避免引入重型框架带来的抽象层调试成本。
- **编排层选型 LangGraph.js 的理由**：checkpoint 断点恢复与状态快照是"可审计原则"的工程化载体；并行分支的 reducer 合并模型省去手写 Promise.all 与去重；节点级流式事件直通 SSE；interrupt 为人工复核预留扩展点。LLM 工具循环仍由 AI SDK 承担，形成"LangGraph 管流程、AI SDK 管单步"的清晰分工。
- **Rolldown 工具链一致性**：前端 Vite 8 与库/CLI 的 tsdown 均以 Rolldown 为内核（Rolldown 1.0 已于 2026-05 稳定），全仓构建行为一致、缓存策略统一。
- **模型选型**：GenAI 代码审查工具对比显示，CodeRabbit 召回率最高（64%），Claude Sonnet 4.5 基线为 61%，Copilot Code Review 为 54%。本项目以 Claude Sonnet 4.5 作为主力 LLM，本地开发环境提供 Ollama 支持（经 OpenAI 兼容端点接入）以降低成本和保护代码隐私。

### 五、Vibe Coding 工作流集成方案

#### 5.1 Git Hook 集成

系统以 `pre-commit` hook 方式集成到 Vibe Coding 工作流中，在开发者执行 `git commit` 时自动触发。

**方案 A：Husky v9（Node.js 生态推荐）**：

```bash
# 仓库内一次性安装
pnpm add -D husky && pnpm exec husky init
```

```bash
# .husky/pre-commit
# fast 模式：目标 ≤15s，BLOCKER 阻断提交
ai-review run --staged --mode fast --block-on BLOCKER
```

**方案 B：pre-commit 框架（Python 生态/多语言团队）**：

```yaml
# .pre-commit-config.yaml
repos:
  - repo: local
    hooks:
      - id: ai-code-review
        name: AI Code Review Agent
        entry: ai-review run --staged --mode fast
        language: system
        stages: [pre-commit]
        pass_filenames: false
        # 阻断条件：发现 BLOCKER 级别问题时阻断提交
        args: [--block-on=BLOCKER]
```

> **Windows 兼容说明**：husky v9 基于 `core.hooksPath` 实现，Windows 下无需额外配置；CLI 的 git 调用均通过 `child_process` 显式传参，规避 shell 转义差异。这是全栈 TypeScript（相比原 Python 方案）带来的直接收益。

**慢审查不阻塞提交**：pre-commit 默认只跑 fast 模式；完整深度审查交给 CI（PR 触发）或 Dashboard 手动触发，两者结果在 Dashboard 中关联展示。

#### 5.2 配置文件设计

```yaml
# .ai-review.yml
llm:
  provider: anthropic # anthropic | openai | ollama
  model: claude-sonnet-4.5
  api_key: ${AI_REVIEW_API_KEY} # 仅支持环境变量引用，禁止明文提交密钥
  max_tokens_per_review: 50000
  temperature: 0.1 # 低温度保证审查一致性

review:
  mode: fast # fast（hook 默认）| full（CI/手动）
  dimensions:
    - correctness # 正确性
    - security # 安全性
    - performance # 性能
    - maintainability # 可维护性
  block_on: BLOCKER # 阻断提交的严重度阈值
  ignore_patterns:
    - '*.lock'
    - 'pnpm-lock.yaml'
    - 'dist/**'
    - '*.min.js'
  enabled_extensions:
    - .py .js .ts .tsx .vue .go .java

rag:
  enabled: true
  knowledge_base_paths:
    - 'docs/'
    - 'CLAUDE.md'
    - 'ARCHITECTURE.md'
  index_dir: '.ai-review-cache/vectors'
  top_k: 5

static_analysis:
  enabled_tools:
    - complexity_check
    - secret_scan
    - dependency_scan
  complexity_threshold: 15 # 圈复杂度阈值

report:
  format: markdown # markdown | html | json
  output_dir: '.ai-review-reports'
  include_praise: true # 包含正面反馈
```

配置加载以 zod schema 严格校验（字段级错误提示，校验失败退出码 2），并递归解析 `${ENV_NAME}` 引用：

```typescript
// packages/shared/src/config.ts
import { z } from 'zod';
import { readFileSync } from 'node:fs';
import YAML from 'yaml';

export const AiReviewConfigSchema = z.object({
  llm: z.object({
    provider: z.enum(['anthropic', 'openai', 'ollama']).default('anthropic'),
    model: z.string().default('claude-sonnet-4.5'),
    apiKey: z.string().default('${AI_REVIEW_API_KEY}'),
    maxTokensPerReview: z.number().int().positive().max(200_000).default(50_000),
    temperature: z.number().min(0).max(2).default(0.1),
  }),
  review: z.object({
    mode: z.enum(['fast', 'full']).default('fast'),
    dimensions: z
      .array(z.enum(['correctness', 'security', 'performance', 'maintainability']))
      .default(['correctness', 'security', 'performance', 'maintainability']),
    blockOn: z.enum(['BLOCKER', 'WARNING', 'NIT']).default('BLOCKER'),
    ignorePatterns: z
      .array(z.string())
      .default(['*.lock', 'pnpm-lock.yaml', 'dist/**', '*.min.js']),
  }),
  rag: z.object({
    enabled: z.boolean().default(true),
    knowledgeBasePaths: z.array(z.string()).default(['docs/']),
    indexDir: z.string().default('.ai-review-cache/vectors'),
    topK: z.number().int().min(1).max(20).default(5),
  }),
  staticAnalysis: z.object({
    enabledTools: z
      .array(z.string())
      .default(['complexity_check', 'secret_scan', 'dependency_scan']),
    complexityThreshold: z.number().int().min(1).default(15),
  }),
  report: z.object({
    format: z.enum(['markdown', 'html', 'json']).default('markdown'),
    outputDir: z.string().default('.ai-review-reports'),
    includePraise: z.boolean().default(true),
  }),
});

export type AiReviewConfig = z.infer<typeof AiReviewConfigSchema>;

export function loadConfig(path = '.ai-review.yml'): AiReviewConfig {
  const raw = YAML.parse(readFileSync(path, 'utf8'));
  return AiReviewConfigSchema.parse(resolveEnvRefs(raw)); // 校验失败 → 字段级提示 + exit(2)
}
```

#### 5.3 与 Claude Code 的协同

在 Vibe Coding 工作流中，`CLAUDE.md` 文件承载项目全局约束。系统自动读取 `CLAUDE.md` 作为审查上下文的一部分，确保审查结果符合项目特定的编码规范和架构约定。开发者可以在 `CLAUDE.md` 中声明审查规则：

```markdown
<!-- CLAUDE.md -->

## 代码审查规则

- 所有数据库查询必须使用参数化查询，禁止字符串拼接
- 所有 API 端点必须包含输入校验
- 异步函数必须处理超时和取消
- 禁止在循环内执行数据库查询
```

此外，系统的工具总线通过 MCP 协议对外暴露（stdio/SSE），Claude Code 等 MCP 客户端可直接复用 `complexity_check`、`secret_scan` 等工具，实现"审查能力"与"开发助手"的共享。

### 六、数据模型设计

```sql
-- 审查记录表
CREATE TABLE reviews (
    id              TEXT PRIMARY KEY,          -- review-20260911-001
    repo_path       TEXT NOT NULL,
    branch          TEXT NOT NULL,
    commit_hash     TEXT,
    author          TEXT,
    status          TEXT DEFAULT 'pending',    -- pending/running/completed/failed
    risk_score      REAL,                      -- 0-100 综合风险评分
    total_findings  INTEGER DEFAULT 0,
    blocker_count   INTEGER DEFAULT 0,
    warning_count   INTEGER DEFAULT 0,
    nit_count       INTEGER DEFAULT 0,
    token_used      INTEGER DEFAULT 0,
    duration_ms     INTEGER,
    created_at      DATETIME DEFAULT CURRENT_TIMESTAMP,
    completed_at    DATETIME
);

-- 审查发现表
CREATE TABLE findings (
    id              INTEGER PRIMARY KEY AUTOINCREMENT,
    review_id       TEXT REFERENCES reviews(id),
    agent           TEXT,                      -- correctness/security/performance
    severity        TEXT NOT NULL,             -- BLOCKER/WARNING/NIT/PRAISE
    confidence      REAL,                      -- 0-1
    file_path       TEXT NOT NULL,
    line_start      INTEGER,
    line_end        INTEGER,
    title           TEXT NOT NULL,
    description     TEXT,
    suggestion      TEXT,
    code_snippet    TEXT,
    cwe_id          TEXT,                      -- CWE-89 等
    is_false_positive INTEGER DEFAULT 0,       -- Dashboard 可标记误报，沉淀为优化数据
    created_at      DATETIME DEFAULT CURRENT_TIMESTAMP
);
CREATE INDEX idx_findings_review ON findings(review_id);

-- 知识库索引表
CREATE TABLE knowledge_index (
    id              INTEGER PRIMARY KEY AUTOINCREMENT,
    source_type     TEXT,                      -- code/doc/history
    file_path       TEXT,
    content_hash    TEXT,                      -- 增量索引：内容哈希去重
    chunk_text      TEXT,
    vector_id       TEXT,                      -- LanceDB 中的行 ID
    metadata        JSON,
    indexed_at      DATETIME DEFAULT CURRENT_TIMESTAMP
);

-- 审查缓存表（内容级去重：相同代码块 + 相同 prompt 版本不重复消耗 LLM）
CREATE TABLE review_cache (
    cache_key       TEXT PRIMARY KEY,          -- hash(blob_sha + agent + prompt_version)
    review_id       TEXT,
    findings_json   TEXT,
    token_saved     INTEGER DEFAULT 0,
    created_at      DATETIME DEFAULT CURRENT_TIMESTAMP
);
```

Drizzle ORM 映射示例（SQLite 与 PostgreSQL 双方言同构，团队版仅切换驱动与表构造器）：

```typescript
// packages/db/src/schema.ts
import { sqliteTable, text, integer, real } from 'drizzle-orm/sqlite-core';

export const reviews = sqliteTable('reviews', {
  id: text('id').primaryKey(),
  repoPath: text('repo_path').notNull(),
  branch: text('branch').notNull(),
  commitHash: text('commit_hash'),
  status: text('status', { enum: ['pending', 'running', 'completed', 'failed'] })
    .default('pending')
    .notNull(),
  riskScore: real('risk_score'),
  blockerCount: integer('blocker_count').default(0).notNull(),
  tokenUsed: integer('token_used').default(0).notNull(),
  durationMs: integer('duration_ms'),
  createdAt: text('created_at').$defaultFn(() => new Date().toISOString()),
});
// findings / knowledgeIndex / reviewCache 同理定义；团队版切换 pgTable 即可
```

### 七、部署方案

#### 7.1 开发环境（本地）

```bash
# 前置要求：Node.js ≥ 22；corepack 按根 package.json 的 packageManager 激活 pnpm 10
corepack enable

# 克隆与安装（workspaces 全量安装）
pnpm install

# 构建（Turborepo 按依赖图调度：packages → apps）
pnpm build

# 初始化配置（交互式向导）
pnpm dlx @ai-review/cli init --provider anthropic

# 构建知识库索引
pnpm dlx @ai-review/cli index --paths docs/ CLAUDE.md src/

# 安装 Git Hook（写入 .husky/pre-commit，Windows/macOS/Linux 通用）
pnpm dlx @ai-review/cli install-hook

# 手动触发完整审查（不提交）
pnpm dlx @ai-review/cli run --staged --mode full

# 本地并行开发（server + web）
pnpm dev
```

#### 7.2 团队环境（Docker Compose）

```dockerfile
# Dockerfile —— multi-stage，corepack 激活 pnpm
FROM node:22-alpine AS build
WORKDIR /app
RUN corepack enable
COPY . .
RUN pnpm install --frozen-lockfile \
 && pnpm turbo build --filter=@ai-review/server... --filter=@ai-review/web...

FROM node:22-alpine
WORKDIR /app
ENV NODE_ENV=production
COPY --from=build /app/node_modules ./node_modules
COPY --from=build /app/apps/server/dist ./apps/server/dist
COPY --from=build /app/apps/web/dist ./apps/web/dist
COPY --from=build /app/packages ./packages
CMD ["node", "apps/server/dist/index.js"]   # Hono 同时托管 API 与 web 静态产物
```

```yaml
# docker-compose.yml
services:
  review-server:
    build: .
    ports:
      - '8080:8080' # API + Web Dashboard（同一端口）
    environment:
      - LLM_PROVIDER=anthropic
      - ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY}
      - DATABASE_URL=postgresql://review:pass@db:5432/review
      - REDIS_URL=redis://redis:6379
    volumes:
      - ./knowledge_base:/app/knowledge_base
      - lancedata:/app/.ai-review-cache/vectors # LanceDB 为嵌入式库，以数据卷持久化
    depends_on:
      - db
      - redis

  db:
    image: postgres:16
    environment:
      POSTGRES_DB: review
      POSTGRES_USER: review
      POSTGRES_PASSWORD: pass
    volumes:
      - pgdata:/var/lib/postgresql/data

  redis:
    image: redis:7-alpine

volumes:
  pgdata:
  lancedata:
```

> **与原方案的差异**：LanceDB 是嵌入式向量数据库（进程内运行），不存在官方容器镜像，原方案的 `lancedb/lancedb:latest` 服务为误配；优化后以数据卷承载其本地索引目录，架构更简单。

#### 7.3 CI/CD 集成

在 GitHub Actions 中集成审查 Agent，对所有 PR 自动执行完整审查并发表评论：

```yaml
# .github/workflows/ai-review.yml
name: AI Code Review
on:
  pull_request:
    types: [opened, synchronize]

jobs:
  review:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0 # 需要 base...HEAD 区间
      - uses: pnpm/action-setup@v4 # 读取 packageManager 字段自动装 pnpm 10
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - name: Run AI Review
        env:
          AI_REVIEW_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
        run: |
          pnpm exec ai-review run \
            --base origin/${{ github.base_ref }} \
            --mode full --block-on BLOCKER --json \
            --output .ai-review-reports/latest.json
      - name: Post Review Comment
        if: always()
        uses: actions/github-script@v7
        with:
          script: |
            const report = require('./.ai-review-reports/latest.json');
            await github.rest.pulls.createReview({
              owner: context.repo.owner,
              repo: context.repo.repo,
              pull_number: context.issue.number,
              body: report.markdown,
              event: report.blocker_count > 0 ? 'REQUEST_CHANGES' : 'COMMENT'
            });
```

## 第二部分：项目报告书

### 一、执行摘要

**项目名称**：AI Code Review Agent — 基于 Vibe Coding 的 Git 提交智能审查与风险分析系统

**项目定位**：面向 Vibe Coding 工作流的自动化代码审查中间件，在开发者执行 `git commit` 时自动触发多维度 AI 审查，识别代码缺陷、安全风险和性能问题，生成结构化审查报告。

**核心价值**：

- 将代码审查从"事后人工审查"前移到"提交前自动审查"，在 Vibe Coding 的 Review 阶段实现自动化
- 通过多 Agent 并行审查 + 混合分析（LLM + 静态分析），在保持高召回率的同时抑制幻觉
- 通过 RAG 知识库注入项目上下文，将业务逻辑类问题的召回率从 21% 提升至 50%+
- **全栈 TypeScript 单语言实现**（React 19 + Vite 8/Rolldown + Hono + pnpm Monorepo）：一份 zod 契约贯通配置、API、数据库与前端，CLI/CI/团队三种形态共享同一套代码，分发与跨平台（含 Windows）成本最低

**目标用户**：使用 Vibe Coding 工作流的个人开发者和中小型研发团队。

**关键指标目标**：

| 指标                                  | 目标值  |
| ------------------------------------- | ------- |
| 注入类漏洞召回率                      | ≥ 65%   |
| 业务逻辑漏洞召回率                    | ≥ 45%   |
| 误报率                                | ≤ 15%   |
| 单次审查耗时（fast 模式，500 行变更） | ≤ 15 秒 |
| 单次审查耗时（full 模式，500 行变更） | ≤ 60 秒 |
| 单次审查成本                          | ≤ $0.05 |

### 二、项目背景与问题定义

#### 2.1 Vibe Coding 时代的代码审查挑战

Vibe Coding 改变了代码的生产方式——AI 生成代码的速度远超人工编写，但生成代码的质量参差不齐。开发者需要一种与 AI 生成速度匹配的审查机制。传统的代码审查流程（发起 PR → 等待人工审查 → 修改 → 重新审查）在 AI 辅助开发场景下成为瓶颈。

与此同时，GenAI 代码审查工具虽然已经成熟，但在以下方面仍需改进：

- 幻觉率平均 18%，部分工具高达 27%
- 对业务逻辑漏洞的检测能力弱（平均召回率 21%）
- 缺乏项目特定上下文的注入能力

#### 2.2 现有方案的不足

| 方案类型    | 代表工具               | 不足                                     |
| ----------- | ---------------------- | ---------------------------------------- |
| 纯静态分析  | SonarQube, CodeQL      | F1 分数低（0.26-0.55），无法理解业务语义 |
| 纯 LLM 审查 | Copilot Code Review    | 幻觉率高，缺乏确定性验证                 |
| 商业 SaaS   | CodeRabbit, Qodo       | 数据需上传第三方，定制性差               |
| 开源工具    | ai-code-review-toolkit | 功能单一，缺乏多 Agent 协作和 RAG 集成   |

本项目的差异化定位：**开源、本地优先、全栈 TypeScript（pnpm Monorepo 单仓交付）、多 Agent 混合分析、深度集成 Vibe Coding 工作流**。

### 三、系统设计与实现

（系统架构、核心模块设计详见第一部分，此处补充关键实现细节）

#### 3.1 ReAct Agent 循环的实现

基于 Vercel AI SDK 5 实现 ReAct 循环：Provider 抽象、工具调用循环、usage 统计由 SDK 承担，审查编排逻辑保留在自有代码中，兼顾开发效率与可控性：

```typescript
// packages/agents/src/review-agent.ts
import { generateText, stepCountIs } from 'ai';
import type { LanguageModel } from 'ai';
import type { CodeContext, Finding, ToolRegistry, TokenBudget } from '@ai-review/shared';

export class ReviewAgent {
  constructor(
    private readonly model: LanguageModel,
    private readonly tools: ToolRegistry, // 进程内注册表 + MCP 双通道
    private readonly systemPrompt: string,
    private readonly opts: {
      maxSteps?: number; // ReAct 最大步数
      signal?: AbortSignal; // 全局取消
      budget?: TokenBudget; // token 硬预算
    } = {},
  ) {}

  async review(context: CodeContext): Promise<Finding[]> {
    const { text, usage } = await generateText({
      model: this.model,
      system: this.systemPrompt,
      prompt: buildReviewPrompt(context), // 注入 diff + 函数级片段 + RAG 上下文
      tools: this.tools.toAISdkTools(), // 工具 → AI SDK tool()（zod 入参）
      stopWhen: stepCountIs(this.opts.maxSteps ?? 10),
      abortSignal: this.opts.signal,
      temperature: 0.1, // 低温度保证审查一致性
    });

    this.opts.budget?.account(usage);

    // 结构化校验：文件存在、行号落在 diff 范围内、字段类型正确；
    // 校验失败的输出不静默丢弃，而是触发自愈回退重新生成
    return parseFindings(text, context);
  }
}
```

#### 3.2 LLM Provider 抽象与降级链

为保证 pre-commit 不因云服务故障而阻塞开发者，Provider 层实现"云端主力 + 本地兜底"的降级链：

```typescript
// packages/llm/src/provider.ts
import { createAnthropic } from '@ai-sdk/anthropic';
import { createOpenAI } from '@ai-sdk/openai';

export function buildModelChain(cfg: AiReviewConfig) {
  const anthropic = createAnthropic({ apiKey: resolveApiKey(cfg) });
  // Ollama 提供 OpenAI 兼容端点，同一套 AI SDK 代码即可接入本地模型
  const local = createOpenAI({
    baseURL: 'http://127.0.0.1:11434/v1',
    apiKey: 'ollama',
  });

  const primary = anthropic(cfg.llm.model);
  const fallback = local('qwen3-coder:30b');

  // 主模型超时/限流/5xx 时自动降级到本地模型（AI SDK middleware 实现，简化示意）
  return withFallback(primary, fallback, { retries: 2, timeoutMs: 30_000 });
}
```

成本策略与降级链呼应：高风险文件走云端主力模型，低风险文件在预算紧张或离线场景下降级到本地模型，极端情况（本地模型也不可用）仅运行静态分析并在报告中明确标注"本次未经 LLM 审查"。

### 四、测试与验证方案

#### 4.1 测试策略

**单元测试（Vitest）**：对 Diff 解析器、风险规划器、复杂度分析器、报告生成器、zod 配置校验等模块进行单元测试，覆盖边界条件（空 diff、二进制文件、超大文件、CRLF/中文路径——Windows 场景重点）。Vitest 与 Vite 8 共享 transform 管线，`packages/*` 的测试在 Turborepo 中并行调度。

**集成测试**：使用预置的代码仓库快照（含正常/异常分支 fixture），在临时目录中执行真实 git 操作（`git init` → `git add` → 触发流水线），端到端验证从 staged diff 到报告生成与退出码的完整链路。LLM 调用以**录制/回放**模式替换（首次真实调用时持久化响应为 fixture，此后回放），保证测试确定性且零 token 消耗。

**前端测试**：组件用 Vitest + Testing Library；API mock 用 MSW；Dashboard 关键路径（查看报告、标记误报）用 Playwright 做冒烟 E2E。

**基准测试**：构建包含已知缺陷的代码测试集（参考 CWE 分类），测量召回率、精确率和 F1 分数。测试集覆盖：

- 注入类：SQL 注入、命令注入、XSS（CWE-89/78/79）
- 认证类：缺失权限校验、硬编码凭证（CWE-287/798）
- 反序列化：不安全 pickle/eval（CWE-502）
- 业务逻辑：权限绕过、竞态条件

#### 4.2 评估指标

| 指标             | 定义                       | 目标            |
| ---------------- | -------------------------- | --------------- |
| 召回率           | 检出缺陷数 / 总缺陷数      | ≥ 60%           |
| 精确率           | 真阳性 / (真阳性 + 假阳性) | ≥ 80%           |
| F1 分数          | 2 × P × R / (P + R)        | ≥ 0.70          |
| 幻觉率           | 无根据的发现数 / 总发现数  | ≤ 15%           |
| 响应时间（fast） | 从 diff 到报告生成         | ≤ 15s（500 行） |
| 响应时间（full） | 从 diff 到报告生成         | ≤ 60s（500 行） |
| Token 效率       | 每千行代码消耗的 token     | ≤ 15K           |

### 五、项目里程碑与交付计划

| 阶段                | 时间        | 交付物            | 关键任务                                                                                                                             |
| ------------------- | ----------- | ----------------- | ------------------------------------------------------------------------------------------------------------------------------------ |
| **Phase 1: MVP**    | 第 1-4 周   | 可运行的 CLI 工具 | pnpm Monorepo 脚手架（workspaces + catalog + turbo）、shared zod 契约、Diff 解析、单 Agent LLM 审查、报告生成、Git Hook 集成         |
| **Phase 2: 增强**   | 第 5-8 周   | 多 Agent 版本     | LangGraph.js 并行审查引擎（fan-out/条件边/checkpoint）、Tree-sitter/ts-morph 静态分析集成、RAG 知识库（LanceDB）、交叉验证与自愈回退 |
| **Phase 3: 工程化** | 第 9-12 周  | 团队版            | Hono API + SSE、React 19 + Vite 8 Dashboard、CI/CD 集成、Prometheus 指标、Docker 部署                                                |
| **Phase 4: 优化**   | 第 13-16 周 | 稳定版            | 基准测试调优、缓存与预算策略打磨、多语言支持扩展（Tree-sitter 语法包）、IDE 插件、文档完善                                           |

### 六、风险与应对

| 风险                                                         | 概率 | 影响 | 应对措施                                                                                      |
| ------------------------------------------------------------ | ---- | ---- | --------------------------------------------------------------------------------------------- |
| LLM API 费用超预期                                           | 中   | 中   | token 预算器硬上限；风险规划器过滤低风险文件；内容哈希缓存去重；Ollama 本地模型降级           |
| 幻觉导致误报影响开发体验                                     | 高   | 高   | 多阶段交叉验证、置信度阈值过滤、可配置的阻断策略、Dashboard 误报标记回流                      |
| 审查速度影响提交体验                                         | 中   | 高   | fast/full 双模式（hook 默认 fast ≤15s）；缓存机制；低风险文件仅静态分析                       |
| 原生依赖跨平台二进制分发（better-sqlite3、@lancedb/lancedb） | 中   | 中   | pnpm 构建脚本审批（approve-builds）；CI 矩阵覆盖三平台；预留纯 JS 降级（libsql + sqlite-vec） |
| Tree-sitter 语法包版本兼容与解析失败                         | 中   | 中   | 锁定语法包版本；解析失败自动降级为行级启发式分析，不阻塞流水线                                |
| TS 工具链版本演进（TS6 → TS7 原生编译器、Vite 大版本升级）   | 中   | 低   | 严格限制工具链耦合面（tsconfig/构建配置层）；跟进 tsgo Beta 验证；锁版本 + 渐进升级           |
| 大型仓库索引耗时                                             | 中   | 中   | 增量索引（内容哈希去重），仅索引变更文件的上下文相关区域                                      |
| 多语言支持复杂度                                             | 高   | 中   | 优先支持 TS/JS/Python/Go，通过 Tree-sitter 语法包渐进扩展                                     |

### 七、成本估算

**LLM API 成本**（以 Claude Sonnet 4.5 为例，按 500 行变更估算）：

| 项目                        | 用量 | 单价  | 成本       |
| --------------------------- | ---- | ----- | ---------- |
| 输入 token（含 RAG 上下文） | ~30K | $3/M  | $0.09      |
| 输出 token（审查发现）      | ~4K  | $15/M | $0.06      |
| **单次审查总计**            |      |       | **~$0.15** |

**优化后成本**（风险规划器过滤 + 内容哈希缓存 + 本地模型降级）：

| 场景                                | 单次成本        |
| ----------------------------------- | --------------- |
| 纯云端 LLM                          | $0.10 - $0.15   |
| 混合模式（高风险云端 + 低风险本地） | $0.03 - $0.06   |
| 纯本地（Ollama + 小模型）           | ~$0（硬件成本） |

**基础设施成本**（团队版）：

- 云服务器（4C8G，单容器托管 API + Dashboard）：~$40/月
- PostgreSQL + Redis 托管：~$30/月
- 总计：~$70/月（5 人团队）

### 八、后续演进方向

1. **Vibe Coding 深度集成**：与 Cursor、Claude Code 等工具的深度联动，在 AI 生成代码后立即触发审查，实现"生成即审查"；工具总线经 MCP 对外开放后，Claude Code 可直接复用本系统的静态分析能力。
2. **强化学习优化**：基于开发者对审查发现的反馈（Dashboard 误报标记、采纳/拒绝），持续优化置信度模型与风险规划器权重。
3. **多仓库知识共享**：在团队或组织范围内共享 RAG 知识库，使一个项目学到的最佳实践惠及所有项目。
4. **形式化验证集成**：将 LLM 审查与轻量级形式化验证结合，对关键路径（如认证、支付）进行数学级正确性证明。
5. **自然语言审查规则**：允许开发者用自然语言定义审查规则（如"所有 API 必须返回统一的错误格式"），系统自动转化为可执行的检查逻辑。
6. **TS 7 原生编译器跟进**：随 TypeScript 7（tsgo）正式发布，将 typecheck/AST 分析迁移到原生编译器 API，获得数量级提速，进一步压缩审查延迟。
